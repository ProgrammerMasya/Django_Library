version: '3'


services:
    web:
      build: .
      ports:
       - '8000:8000'
      depends_on:
       - db
      volumes:
       - .:/src

    db:
      image: "postgres:11-alpine"
      expose:
        - 5432
      volumes:
        - /pgdata:/var/lib/postgresql/10/data